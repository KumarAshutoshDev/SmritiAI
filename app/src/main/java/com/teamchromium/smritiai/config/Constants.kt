package com.teamchromium.smritiai.config

/**
 * Shared project constants.
 *
 * FR-FR-03: the face-match confidence threshold must be defined once and
 * referenced everywhere. Do not hardcode 0.80 or 80 anywhere else.
 */
const val FACE_MATCH_CONFIDENCE_THRESHOLD = 0.80f
